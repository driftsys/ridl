[package]
name = "ridl-backend-rust"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
minijinja = "2.5.0"
prettyplease.workspace = true
proc-macro2.workspace = true
quote.workspace = true
ridl-ir = { path = "../../crates/ridl-ir" }
serde.workspace = true
syn.workspace = true

[dev-dependencies]
insta.workspace = true
# Reads the `ridl-sem` Appendix A golden as IR, so the exit-criterion compile
# proof runs on that crate's real lowering rather than a hand-built copy.
serde_json.workspace = true
tempfile = "3.15.0"
