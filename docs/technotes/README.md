# Technotes

Explanatory, informative notes. Nothing here is normative and nothing depends on
it — for decisions that bind downstream work, see
[`../decisions/`](../decisions/).

- **walking-skeleton-architecture.md** — the epic E0 crate map, the end-to-end
  pipeline contract, and the cross-seam facts the E1 implementer needs, as they
  actually landed in the merged code.
