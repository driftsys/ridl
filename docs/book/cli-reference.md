# CLI reference

**This page is not written yet.** It is a placeholder holding a slot in the
book's structure, and it contains no command documentation.

When it is written, it will document every subcommand of `ridl` and `ridlc`:
arguments, options, exit codes, and the diagnostics each can produce.

Until then:

- [Getting started](getting-started.md) introduces the commands in the order
  you need them, and lists what each one does.
- `ridl --help` and `ridl <command> --help` are authoritative for the shipped
  surface. So are `ridlc --help` and `ridlc <command> --help`.
