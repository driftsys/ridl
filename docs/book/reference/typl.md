> **Built.** typl has a working toolchain in this repository (epic E1): the
> compiler accepts every construct below, and `ridl fmt`, the LSP server and the
> code generators are built on it. See
> [what is built](../introduction.md#what-is-built).

{{#include ../../specification/typl-language-reference.md}}
