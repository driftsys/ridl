> **Specified, not built.** No compiler in this repository accepts uxdl. The
> parser has two profiles, typl and ridl, so a `.uxdl` file is not recognised as
> uxdl at all. This reference is complete enough to design against; nothing below
> can be compiled today. See
> [what is built](../introduction.md#what-is-built).

{{#include ../../specification/uxdl-language-reference.md}}
