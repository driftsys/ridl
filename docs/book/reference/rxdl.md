> **Specified, not built.** No compiler in this repository accepts rxdl. The
> parser has two profiles, typl and ridl, so a `.rxdl` file is not recognised at
> all. This reference lands as normative with epic E7a, and ridl's
> boundary-model core (E3) is its precondition. Nothing below can be compiled
> today. See [what is built](../introduction.md#what-is-built).

{{#include ../../specification/rxdl-language-reference.md}}
