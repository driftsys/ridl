> **Specified, not built.** No compiler in this repository accepts rmdl. The
> parser has two profiles, typl and ridl, so a `.rmdl` file is not recognised as
> rmdl at all. This reference is complete enough to design against; nothing below
> can be compiled today, and none of the execution semantics it describes is
> implemented. See [what is built](../introduction.md#what-is-built).

{{#include ../../specification/rmdl-language-reference.md}}
